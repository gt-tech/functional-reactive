package week1.lecture8

import scala.reflect.internal.Variance

object Signals {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(124); 
  println("Welcome to the Scala worksheet");$skip(30); 
  
  val a: Variance = Var(2);System.out.println("""a  : scala.reflect.internal.Variance = """ + $show(a ))}
}
